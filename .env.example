# Variables pour docker-compose (dev local). Copier en .env (ignoré par git).
POSTGRES_USER=median
POSTGRES_PASSWORD=median_dev
POSTGRES_DB=median-db

# Secret JWT du backend (dev uniquement)
JWT_SECRET=dev_jwt_secret_change_me
